//Retorne a url da página atual usando o objeto window
const hrefPag = window.location.href;
console.log(hrefPag);

//Selecionar o primeiro elemento da página que possui a classe 'ativo'
const elementoAtivo = document.querySelector('.ativo'); //Seleciona o primeiro

//Retorne a linguagem do navegador

const languagePag = window.navigator.language;
console.log(languagePag);


//Retorne a largura da pagina
const widthPage = window.innerWidth;
console.log(widthPage);